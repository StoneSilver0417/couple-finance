# Couple Finance - Handoff

## 현재 상태

- **버전**: v0.6.4 + 연간 요약 페이지 Phase 1 구현·검증·배포 완료
- **빌드 상태**: 성공 (`npx tsc --noEmit`, `npx eslint .` 0 오류·0 경고, `npm run build` 통과, 실제 Supabase 연동 E2E 21/21 통과, 프로덕션 브라우저 시각 검증 완료)
- **배포 상태**: 커밋 `86e42a0` 푸시 완료, Vercel 프로덕션 배포 `Ready` 확인
- **프로덕션 URL**: https://couple-finance-roan.vercel.app
- **깃허브 저장소**: **PUBLIC 전환 완료** (2026-07-23) — https://github.com/StoneSilver0417/couple-finance
- **Supabase**: 개발/운영 분리 없이 `ieahmpxiaamesrnfgbng.supabase.co` 단일 프로젝트 사용

## 최근 작업

- **연간 요약 Phase 1 브라우저 시각 검증 + 배포 완료 (2026-07-24, Claude Code)**: 코덱스가 인앱 브라우저 자동화 연결이 끊겨 멈춘 지점을 이어받았다. 로컬 tsc/eslint/build 재확인 후 커밋 2건(`4e213aa`, `86e42a0`)을 `master`에 푸시해 Vercel 프로덕션 배포 완료. E2E 전용 계정으로 프로덕션에서 직접 확인: 연간 수입/지출/잔액/예산사용률 카드 값이 CHANGELOG 기댓값과 정확히 일치, 지출→수입 추이 토글 정상 동작, 카테고리 도넛(8개, 74.1%~1.2%) 정상 렌더, 데이터 없는 2025년은 예산카드·도넛이 에러 없이 자동 숨김, 분석 페이지 헤더의 "연간 요약" 버튼이 올바른 연도로 이동, 잘못된 연도(`abcd`)와 미래 연도(`2027`) 모두 현재 연도로 즉시 redirect. 콘솔 에러 0건(기존에도 있던 recharts 컨테이너 크기 경고만 존재).
- **연간 요약 페이지 Phase 1 구현 (2026-07-23)**: 조회 전용 `/transactions/annual/[year]` 페이지를 추가했다. 연간 수입·지출·잔액·변동지출 기준 예산 사용률, 월별 수입/지출 추이, 카테고리별 지출 비중을 기존 DB와 차트 컴포넌트로 제공한다. 분석 화면에 진입 버튼을 추가했고 모바일 헤더·차트 여백도 정렬했다. 신규 마이그레이션과 npm 의존성은 없다.
- **연간 요약 E2E 완료 (2026-07-23)**: 전용 테스트 계정에 2026년 거래 20건·월별 잔액 6개월·월 예산 6개월을 입력해 실제 Supabase → 로컬 Next.js 서버 경로를 검증했다. 연간 합계·변동지출 예산 사용률·추이/도넛·분석 화면 진입점·빈 연도·연도 검증·비로그인 이동 등 21개 단언이 모두 통과했다. 계정 정보는 Git에서 제외되는 `.env.local`의 `E2E_TEST_EMAIL`/`E2E_TEST_PASSWORD`에만 보관한다.

## 알려진 이슈

- **관리자 화면 실사용 재확인 필요**: 사용자가 가입일 교정 SQL 적용을 완료했다. 배포 후 실제 기기에서 가입일과 카드 정렬이 의도대로 보이는지 확인이 필요하다.
- **월간 AI 보고서 실사용 E2E 미완료**: 인증 보호는 확인했으나 정상 Gemini 키를 통한 생성 성공 경로는 아직 실사용 검증하지 못했다.
- **자산 변동 기록 스냅샷 미갱신**: 같은 날 여러 번 자산을 수정하면 `asset_history.total_net_worth`가 최초 값에 고정된다. RLS UPDATE 경로 확인이 필요하다.
- **운영 DB 테스트 데이터 정리 필요**: E2E 테스트 계정 2개("Test Household" 가구, 버그검증용 가구) 관련 Auth·가구 데이터를 삭제해야 한다 (계정 식별 정보는 공개 저장소 전환에 따라 이 문서에서 제거 — Supabase Dashboard의 Authentication > Users에서 가장 최근 테스트 계정들 확인).
  - **주의**: README 스크린샷용 데모 가구("도준이네 가계부", 거래 14건·자산 4건·예산 250만원)도 생성됨 — 단, **스크린샷 재촬영에 재사용 가치가 있으므로 당장 지우지 말고 유지 권장**. 지울 경우 위 계정들과 동일한 방식으로 정리.
  - **연간 요약 E2E 전용 계정**도 생성됨 — 프로덕션 시각 검증까지 완료했으므로 이제 정리 대상(위 계정들과 동일한 방식). 자격 정보는 로컬 `.env.local`(`E2E_TEST_EMAIL`/`E2E_TEST_PASSWORD`)에만 있다.
- Next.js 16 middleware → proxy 경고가 있으나 기능 영향은 없다.

## 다음 TODO

1. [ ] 실제 보고서에서 예산 사용률이 홈 지출 분석과 같은지 확인하고, 기존 보고서는 필요하면 재생성해 AI 피드백 전체를 갱신한다.
2. [ ] 설치된 PWA는 아이콘 캐시가 남을 수 있으므로 재설치 후 신규 홈 화면 아이콘을 확인한다.
3. [ ] 프로덕션에서 정상 Gemini 키 생성 시간을 실사용 검증한다.
4. [ ] 자산 변동 기록 스냅샷 미갱신 버그를 Supabase 직접 조회로 조사·수정한다.
5. [ ] 운영 DB 테스트 데이터와 Auth 계정 3개(테스트 2개 + 연간 요약 E2E 전용 계정)를 정리한다.
6. [ ] (나중, 사용자 요청 시) AI 보고서 분기/반기/연간 확장 (Phase 2) — 같은 지시서 문서 참고.
